from setuptools import setup, find_packages

setup(
    name="Coupling-Flow-Bound",
    version="1.0",
    description="Code and experiments for the convergence rate bound for coupling-based normalizing flows on the non-Standardness",
    url="https://github.com/vislearn/Coupling-Flow-Bound",
    author="Felix Draxler",
    author_email="felix.draxler@iwr.uni-heidelberg.de",
    install_requires=[],
    setup_requires=[],
    packages=find_packages(),
    ext_package="",
)
